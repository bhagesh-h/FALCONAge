# Per-clock coverage and skip reasons

Per-clock coverage and skip reasons

## Usage

``` r
coverage(x)

# S3 method for class 'falcon_result'
coverage(x)
```

## Arguments

- x:

  A `falcon_result`.

## Value

A data frame.

## Examples

``` r
if (FALSE) { # \dontrun{
coverage(res)
} # }
```
