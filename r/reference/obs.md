# Sample annotation

Sample annotation

## Usage

``` r
obs(x)

# S3 method for class 'falcon_data'
obs(x)

# S3 method for class 'falcon_result'
obs(x)
```

## Arguments

- x:

  A `falcon_data` or `falcon_result`.

## Value

A data frame, one row per sample.

## Examples

``` r
if (FALSE) { # \dontrun{
head(obs(d))
} # }
```
