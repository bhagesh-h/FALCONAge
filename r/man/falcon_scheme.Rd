% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{falcon_scheme}
\alias{falcon_scheme}
\title{The shared visual specification}
\usage{
falcon_scheme(path = NULL, reload = FALSE)
}
\arguments{
\item{path}{Optional path to a colour scheme file.}

\item{reload}{Force a re-read, after editing the file in a live session.}
}
\value{
A nested list: \code{palette}, \code{theme}, \code{plots}.
}
\description{
Reads \code{colorscheme.yaml} -- the single file that decides how every FALCONAge
figure looks and what it says, in both languages.
}
\details{
Searched for in three places, in order: the \code{path} argument, the
\code{FALCONAGE_COLORSCHEME} environment variable, then the copy that ships inside
the Python core. That order lets you restyle a whole report without touching
the installation.
}
\examples{
\dontrun{
sch <- falcon_scheme()
sch$palette$categorical
sch$plots$ba_vs_ca$description
}
}
