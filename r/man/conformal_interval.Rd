% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{conformal_interval}
\alias{conformal_interval}
\title{Distribution-free prediction intervals against chronological age}
\usage{
conformal_interval(x, level = 0.9, clocks = NULL)
}
\arguments{
\item{x}{A \code{falcon_result}.}

\item{level}{Coverage. One of 0.80, 0.90, 0.95.}

\item{clocks}{Optional clock names; only age-scale clocks are calibrated.}
}
\value{
A data frame with value, lo, hi, half_width, median_bias and mae.
}
\description{
A different question from \code{\link[=technical_se]{technical_se()}}, with a larger answer. Technical
error asks what a repeat of the same DNA would do; this asks how far the
number is likely to be from the truth.
}
\details{
Split conformal: the half-width is a quantile of the absolute residual on a
calibration set of healthy blood samples with known ages, so on any sample
exchangeable with that cohort the interval covers the truth at the stated
rate, with no distributional assumption and a finite-sample guarantee.
}
\section{The limit, which is not small}{

Coverage holds for data \emph{exchangeable with the calibration cohort} -- public
blood data, adult, overwhelmingly of European ancestry. On a paediatric or
non-European cohort the guarantee does not transfer, which is why every row
carries \code{exchangeable = FALSE}: nothing here can verify it, so nothing here
implies it.
}

