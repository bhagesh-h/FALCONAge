% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{run_benchmark}
\alias{run_benchmark}
\alias{print.falcon_benchmark}
\title{The AA1 and AA2 benchmark}
\usage{
run_benchmark(
  x,
  condition_col = "condition",
  control = "HC",
  dataset_col = NULL,
  age_col = "age",
  alpha = 0.05
)

\method{print}{falcon_benchmark}(x, ...)
}
\arguments{
\item{x}{A \code{falcon_benchmark}.}

\item{condition_col, control}{Column naming the condition, and the value that
marks a control.}

\item{dataset_col}{Column naming the study, when several are combined.}

\item{age_col}{Chronological age column.}

\item{alpha}{FDR threshold.}

\item{...}{Ignored.}
}
\value{
A list with \code{summary} (per clock) and \code{per_dataset} (per comparison).
}
\description{
Tests a clock the way the field now expects: does it show higher age
acceleration in people with an aging-accelerating condition than in their own
controls? Median absolute error against chronological age does not answer
that -- a perfect chronological oracle would score best on it and be useless.
}
\details{
\strong{AA2}, for a dataset with controls: is the condition group's acceleration
higher than its controls'? One-sided Mann-Whitney, BH corrected.

\strong{AA1}, for a dataset without controls: is it above zero? One-sided
Wilcoxon signed-rank.

\strong{MedE}, the median signed error on healthy controls, discounts the AA1
credit: \code{total = AA2 + AA1 * (1 - max(0, MedE) / MedAE)}. Without it a clock
that simply over-predicts everybody sweeps AA1, because every group looks
accelerated when the baseline is wrong.
}
\examples{
\dontrun{
b <- run_benchmark(res, condition_col = "condition", control = "HC",
                   dataset_col = "dataset")
b$summary
}
}
