% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{falconage_install}
\alias{falconage_install}
\title{Install the Python core FALCONAge computes with}
\usage{
falconage_install(
  envname = "r-falconage",
  method = c("auto", "virtualenv", "conda"),
  extras = c("methylation", "plot", "anndata"),
  gpu = FALSE,
  cuda = "cu124",
  version = NULL,
  ...
)
}
\arguments{
\item{envname}{Environment name. The default keeps it out of the way of any
other reticulate project on the machine.}

\item{method}{\code{"auto"}, \code{"virtualenv"} or \code{"conda"}, passed to reticulate.}

\item{extras}{Optional extras to install alongside the core.
\code{"methylation"} adds the parquet reader; \code{"plot"} adds matplotlib;
\code{"anndata"} adds the \code{.h5ad} reader. Note that \code{"gpu"} is deliberately
\emph{not} in this list -- see \code{gpu} below.}

\item{gpu}{Install CUDA torch as well. Off by default, and not merely out of
caution: on the clocks that ship today the GPU is slower than the CPU
(0.58 s against 3.74 s at 16,384 samples on an RTX 4060), because a linear
clock over a few thousand features is too small a matrix multiplication to
pay for the transfer. It earns its place on the PC clocks and on neural
architectures, none of which are tier A yet.}

\item{cuda}{CUDA version for the torch wheel index, e.g. \code{"cu124"}. The
\code{gpu} extra alone is not enough: pip's default index serves a CUDA build on
Linux and a CPU-only build on Windows under the same name, so the wheel has
to be requested from PyTorch's own index explicitly.}

\item{version}{Git ref to install from. A version number such as \code{"1.0.0"}
becomes the tag \code{v1.0.0}; anything else -- \code{"main"}, a branch, a commit
SHA -- is used verbatim. Defaults to the version of this R package.}

\item{...}{Passed to \code{\link[reticulate:py_install]{reticulate::py_install()}}.}
}
\value{
Invisibly, the resolved configuration from \code{\link[=falconage_config]{falconage_config()}}.
}
\description{
Creates a managed virtual environment and installs the \code{falconage} Python
package into it. Run once per machine; \code{\link[=falconage_config]{falconage_config()}} reports what it
resolved to.
}
\section{Why an environment of its own}{

The alternative -- installing into whatever interpreter reticulate finds --
works until the day something else in that interpreter upgrades numpy, and
then the same script gives a different answer in the seventh decimal with no
change to anything the user can see. A named environment makes the
dependency set a property of the package rather than of the machine.
}

\section{Where the core comes from}{

FALCONAge is not on PyPI, so this installs from the GitHub repository. The
Python package sits in the \verb{python/} subdirectory, which the
\verb{#subdirectory=python} fragment selects, and the ref is pinned to the tag
matching this R package's version rather than left on \code{main}. Those two
halves have to agree: the R suite asserts bit equality against the core, and
a mismatched pair fails that assertion in a way that reads as a numerical bug
rather than as version skew.
}

\examples{
\dontrun{
falconage_install()
falconage_install(extras = c("plot", "anndata"))
falconage_install(gpu = TRUE, cuda = "cu124")
falconage_install(version = "main")   # track the development branch
}
}
