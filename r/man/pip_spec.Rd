% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge.R
\name{pip_spec}
\alias{pip_spec}
\title{The pip specifier for the Python core}
\usage{
pip_spec()
}
\value{
A single string suitable for \verb{pip install}.
}
\description{
FALCONAge is not on PyPI, so \verb{pip install falconage} finds nothing (or, worse,
finds some unrelated name). The core lives in the \verb{python/} subdirectory of
the GitHub repository, which is what the \verb{#subdirectory=} fragment selects.
}
\details{
The ref is pinned to \verb{v<Version>} from DESCRIPTION rather than left on
\code{main}, so an R package built from a tag installs the Python core from the
same tag. Those two halves have to agree: the R package asserts bit equality
against the core, and a mismatched pair fails that assertion in a way that
looks like a numerical bug rather than a version skew.
}
\keyword{internal}
