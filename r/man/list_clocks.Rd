% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clocks.R
\name{list_clocks}
\alias{list_clocks}
\title{List the clock catalogue}
\usage{
list_clocks(
  tier = NULL,
  data_type = NULL,
  generation = NULL,
  untraced = FALSE,
  search = NULL
)
}
\arguments{
\item{tier}{\code{"bundled"}, \code{"untraced"} or \code{"licensed"}, or \code{NULL} for all.
The retired \code{"A"}, \code{"B"}, \code{"C"} are still accepted. Bundled ships with
coefficients and runs offline; untraced is catalogued but has no traced
primary source yet; licensed is implemented but its coefficients are
research-use-only.}

\item{data_type}{\code{"dna_methylation"} or \code{"clinical_chemistry"}.}

\item{generation}{\code{"first"}, \code{"second"}, \code{"pace"}, \code{"causal"}, \code{"mitotic"},
\code{"system"} or \code{"other"}.}

\item{untraced}{Only clocks with no established primary source.}

\item{search}{Substring match over id, name, what it predicts, and citation.}
}
\value{
A data frame, one row per clock.
}
\description{
List the clock catalogue
}
\examples{
\dontrun{
list_clocks(tier = "bundled")
list_clocks(search = "mortality")
list_clocks(tier = "licensed")   # the ones needing author permission
}
}
