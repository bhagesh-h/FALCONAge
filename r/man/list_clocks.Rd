% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clocks.R
\name{list_clocks}
\alias{list_clocks}
\title{List the clock catalogue}
\usage{
list_clocks(
  tier = NULL,
  data_type = NULL,
  generation = NULL,
  untraced = FALSE,
  search = NULL
)
}
\arguments{
\item{tier}{\code{"A"}, \code{"B"} or \code{"C"}, or \code{NULL} for all. A ships with
coefficients and runs offline; B is catalogued but has no traced primary
source yet; C is a scaffold whose coefficients are research-use-only.}

\item{data_type}{\code{"dna_methylation"} or \code{"clinical_chemistry"}.}

\item{generation}{\code{"first"}, \code{"second"}, \code{"pace"}, \code{"causal"}, \code{"mitotic"},
\code{"system"} or \code{"other"}.}

\item{untraced}{Only clocks with no established primary source.}

\item{search}{Substring match over id, name, what it predicts, and citation.}
}
\value{
A data frame, one row per clock.
}
\description{
List the clock catalogue
}
\examples{
\dontrun{
list_clocks(tier = "A")
list_clocks(search = "mortality")
list_clocks(tier = "C")   # the ones needing author permission
}
}
