% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{score}
\alias{score}
\alias{falcon_result}
\alias{print.falcon_result}
\alias{summary.falcon_result}
\title{Score a dataset against one or more clocks}
\usage{
score(
  data,
  clocks = "compatible",
  device = "auto",
  dtype = NULL,
  imputation = c("reference", "mean", "none"),
  min_coverage = 0.8,
  reference = NULL
)

\method{print}{falcon_result}(x, ...)

\method{summary}{falcon_result}(object, ...)
}
\arguments{
\item{data}{A \code{falcon_data}.}

\item{clocks}{\code{"compatible"} scores everything this dataset can support and
reports the rest as skipped with a reason. \code{"all"} attempts every clock of
the right modality and fails loudly on the ones that cannot run. A
character vector names them explicitly, and then every name must work -- an
explicit request is never silently dropped.}

\item{device}{\code{"auto"}, \code{"cpu"}, \code{"cuda"} or \code{"mps"}. Naming a device that is
not present is an error rather than a silent downgrade: a run that was
asked for a GPU and quietly used a CPU looks like a very slow success.
\code{"auto"} is CPU even where CUDA exists, which is measured rather than
cautious: on the clocks that ship, the transfer costs more than the dot
product it feeds. A named device is not granted to every clock either --
the three clinical formulas compute in numpy whatever is asked, because
nine markers are not worth a kernel launch. \code{manifest(res)$compute} records
what each clock actually used.}

\item{dtype}{\code{NULL} (float64), \code{"float64"} or \code{"float32"}. Clocks flagged
\code{requires_fp64} in the registry override a float32 request, with a warning.}

\item{imputation}{How to fill a clock feature the data does not carry.
\code{"reference"} uses the value the clock's authors published where one
exists; \code{"mean"} uses the column mean; \code{"none"} refuses, so the coverage
check fails loudly instead of being papered over. Zero is never used --
in beta space it means completely unmethylated, which is a real and extreme
measurement.}

\item{min_coverage}{Fraction of a clock's features that must be present.}

\item{reference}{A reference fitted with \code{\link[=fit_kdm]{fit_kdm()}} or \code{\link[=fit_hd]{fit_hd()}}, for the
two clinical clocks that have no fixed coefficients.}

\item{x}{A \code{falcon_result}.}

\item{...}{Ignored.}

\item{object}{A \code{falcon_result}.}
}
\value{
A \code{falcon_result}: an S3 object wrapping the scores, the per-clock
coverage and the run manifest.
}
\description{
Score a dataset against one or more clocks
}
\examples{
\dontrun{
res <- score(d, clocks = "compatible")
res <- score(d, clocks = c("horvath2013", "dnamphenoage"), device = "cuda")
}
}
