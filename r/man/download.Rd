% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download}
\alias{download}
\title{Download public data by accession}
\usage{
download(accession, want = NULL, dry_run = FALSE)
}
\arguments{
\item{accession}{An accession or URL.}

\item{want}{For GEO series: \code{"matrix"} (the default, metadata and values in
one file), \code{"suppl"}, or \code{"both"}.}

\item{dry_run}{List what would be fetched, and how much, without fetching.
Worth using first on a large series: a supplementary directory can be
several gigabytes.}
}
\value{
A list with \code{files} (paths), \code{samples} (a data frame, when the source
provides one) and \code{notes}.
}
\description{
Dispatches on the shape of the accession: \verb{GSE*}/\verb{GSM*} to GEO, \verb{E-MTAB-*} to
ArrayExpress, a DOI to Zenodo, \code{owner/name} to Hugging Face, or a full https
URL fetched directly.
}
\details{
Credentialed archives -- dbGaP, EGA, Synapse, UK Biobank -- are documented
rather than automated. The access agreement is between you and the archive,
and a tool that made it one function call would be inviting people to breach
it. Once the files are local, read them with \code{\link[=read_betas]{read_betas()}} or
\code{\link[=read_series_matrix]{read_series_matrix()}}.
}
\examples{
\dontrun{
d <- download("GSE182991")
d$files
head(d$samples)

download("GSE182991", want = "suppl", dry_run = TRUE)
}
}
