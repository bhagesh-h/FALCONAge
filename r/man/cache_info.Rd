% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{cache_info}
\alias{cache_info}
\alias{clear_cache}
\title{Inspect or clear the download cache}
\usage{
cache_info()

clear_cache(confirm = FALSE)
}
\arguments{
\item{confirm}{Required for \code{clear_cache()}. Without it the function reports
how much would be deleted and stops -- a cache holding a re-downloadable
gigabyte is still an hour of somebody's time.}
}
\value{
\code{cache_info()} a data frame; \code{clear_cache()} the bytes freed.
}
\description{
The cache is content-addressed by URL, so two accessions that share a file
share the copy.
}
\examples{
\dontrun{
cache_info()
clear_cache(confirm = TRUE)
}
}
