% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{prepare}
\alias{prepare}
\title{Standard methylation preprocessing}
\usage{
prepare(data, aggregate_epicv2 = TRUE, clip = TRUE)
}
\arguments{
\item{data}{A \link{falcon_data}.}

\item{aggregate_epicv2}{Collapse EPIC v2 replicate probes.}

\item{clip}{Clip values into \verb{[0, 1]}.}
}
\value{
A \link{falcon_data}.
}
\description{
Harmonises probe identifiers, collapses EPIC v2 replicate suffixes, clips to
the beta range, and identifies the platform.
}
\details{
The EPIC v2 step is not optional in effect. Illumina renamed \code{cg00000029} to
\code{cg00000029_TC21}, and every clock in the registry matches on the bare
identifier -- so without aggregation an EPIC v2 dataset overlaps almost
nothing, the imputation step fills everything, and the clock returns a
confident number computed from imputed values rather than an error.
}
\examples{
\dontrun{
d <- prepare(read_series_matrix("GSE330325_series_matrix.txt.gz"))
}
}
