% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{read_betas}
\alias{read_betas}
\title{Read a beta matrix}
\usage{
read_betas(path, samples_are = "auto", obs = NULL)
}
\arguments{
\item{path}{CSV, TSV or parquet. Gzip is handled by extension.}

\item{samples_are}{\code{"auto"}, \code{"rows"} or \code{"columns"}. Auto decides from which
axis carries probe-shaped identifiers, which is reliable because \code{cg} ids
are unmistakable -- guessing from the shape alone gets a 1000-sample 450K
matrix right and a 500,000-probe cohort wrong.}

\item{obs}{Optional per-sample annotation, row names matching the samples.}
}
\value{
A \link{falcon_data}.
}
\description{
Read a beta matrix
}
\examples{
\dontrun{
d <- read_betas("betas.csv")
d <- read_betas("betas.parquet", obs = pheno)
}
}
