% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Volcano plot of association results}
\usage{
plot_volcano(assoc, effect = "beta", p = "p", fdr = 0.05, label_top = 10)
}
\arguments{
\item{assoc}{Output of \code{\link[=associate]{associate()}}.}

\item{effect, p}{Column names for the effect size and p-value.}

\item{fdr}{False-discovery rate for the significance threshold.}

\item{label_top}{How many of the strongest hits to label.}
}
\value{
A ggplot.
}
\description{
Effect size against evidence, for the table \code{\link[=associate]{associate()}} returns.
}
\section{The threshold that is drawn}{

The dashed line is the Benjamini-Hochberg cut at \code{fdr}, taken from the \code{q}
column rather than recomputed. Drawing a raw p-value cut instead is the
common error: across many tests the two differ by orders of magnitude, and
the raw one calls noise significant.
}

\examples{
\dontrun{
plot_volcano(associate(res, "mortality"))
}
}
