% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge.R
\name{as_df}
\alias{as_df}
\title{Convert a Python pandas object to an R data frame}
\usage{
as_df(x, row_names = TRUE)
}
\arguments{
\item{x}{A pandas DataFrame or Series proxy.}

\item{row_names}{Keep the pandas index as row names.}
}
\value{
A data frame.
}
\description{
Goes through plain Python lists rather than letting reticulate convert the
DataFrame directly. That is deliberate: reticulate's pandas conversion is
registered against a class name that has changed between releases (1.42
reports \code{pandas.DataFrame} where earlier versions reported
\code{pandas.core.frame.DataFrame}), so \code{py_to_r()} on a DataFrame silently
returns the proxy untouched on some installations and a data frame on
others. A package whose return type depends on the user's reticulate version
is not usable, and the failure is quiet -- \code{nrow()} on the proxy gives
\code{NULL}, not an error.
}
\details{
\code{to_dict("list")} and \code{index.tolist()} are stable across every pandas and
reticulate version in use, at the cost of one extra copy.
}
\keyword{internal}
