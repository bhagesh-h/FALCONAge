# FalconConfig { #falconage.FalconConfig }

```python
FalconConfig(
    device='auto',
    dtype='float64',
    verbose='inform',
    registry_version=REGISTRY_VERSION,
    cache_dir=default_cache_dir(),
    imputation='reference',
    min_coverage=0.8,
    offline=False,
)
```

